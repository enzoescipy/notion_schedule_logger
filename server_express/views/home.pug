extends layout

block scpt
    script.
        function change_desc()
        {
            document.write($("#desc").text());
        }
    

block content
    h1 #{title}
    |
    |
    div(id="desc") description
    |
    |
    input(class="button", type="submit", value="메세지", onclick="change_desc()")
